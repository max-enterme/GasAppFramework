namespace EventSystem {
    export const Facade = {
        Trigger: EventSystem.Trigger,
        Workflow: EventSystem.Workflow,
        Schedule: EventSystem.Schedule,
        Adapters: EventSystem.Adapters,
    };
}
